Currently there are problems with deps (I'll fix later)
In order to install deps run `npm install --legacy-peer-deps`
To run the app `npm start`

- add option `--open` to automatically open the app on browser
